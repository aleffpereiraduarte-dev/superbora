{
    "require": {
        "phpmailer/phpmailer": "^7.0",
        "pusher/pusher-php-server": "^7.2",
        "minishlink/web-push": "^10.0",
        "sentry/sentry": "^4.20"
    },
    "require-dev": {
        "phpunit/phpunit": "^11"
    },
    "autoload-dev": {
        "psr-4": {
            "SuperBora\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit --colors=always",
        "test:coverage": "phpunit --coverage-text"
    }
}
